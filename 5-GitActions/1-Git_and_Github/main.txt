1. Setting up GIT:
    git config --global user.name adesinna

    git config --global user.email aladesaea@yahoo.com

    To see all the configuration

    git config --list

2. Initializing a project:
    git init # this initialize a project locally


3. Git commands

    git status
        This allows you to see the status of the files git is tracking


    git add
        This allows you to add file to the staging area, files here are the
        ones that a snapshot (i.e commit)

            git add .

            git add filename


    git commit
        This is the snapshot, there must be